Every change must be validated
when changes are  in go service or bff:
* run all tests
* run golangci-lint
* run go vet

When changes are in demo-ui/ui
* run yarn test
* run lint

use golint as $(go env GOPATH)/bin/golangci-lint