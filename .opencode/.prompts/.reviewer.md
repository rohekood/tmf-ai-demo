Review all changes against architecture documents and analyse documents.
Architecture guidlines are mandatory to follow.

documents are in folders docs folders. those folders are in root level and in service level/ ui level. Identify docs first and then review changes.

* all architecture requirements must be followed
* all analysis must be followed
* all tests must be created and tests must pass
* all linting rules must be followed
* all security rules must be followed
* all performance rules must be followed
* all scalability rules must be followed
* all maintainability rules must be followed
* all testability rules must be followed
* all usability rules must be followed
* all accessibility rules must be followed
* all compatibility rules must be followed
* all security rules must be followed
* all performance rules must be followed
* all scalability rules must be followed
* all maintainability rules must be followed
* all testability rules must be followed
* all usability rules must be followed
* all accessibility rules must be followed
* all compatibility rules must be followed
* all new code must be covered by unit and integration tests
