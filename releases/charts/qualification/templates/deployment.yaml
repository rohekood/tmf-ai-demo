apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "qualification.fullname" . }}
  labels:
    {{- include "qualification.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "qualification.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "qualification.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: POSTGRES_URL
              {{- if .Values.env.postgres.value }}
              value: {{ .Values.env.postgres.value | quote }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.secretName }}
                  key: {{ .Values.env.postgres.secretKey }}
              {{- end }}
            - name: RABBITMQ_URL
              {{- if .Values.env.rabbitmq.value }}
              value: {{ .Values.env.rabbitmq.value | quote }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.secretName }}
                  key: {{ .Values.env.rabbitmq.secretKey }}
              {{- end }}
            - name: REDIS_ADDR
              value: {{ default .Values.env.redisAddr .Values.env.redis.addr | quote }}
            {{- if or .Values.env.redis.password .Values.env.redis.passwordSecretKey }}
            - name: REDIS_PASSWORD
              {{- if .Values.env.redis.password }}
              value: {{ .Values.env.redis.password | quote }}
              {{- else }}
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.env.secretName }}
                  key: {{ .Values.env.redis.passwordSecretKey }}
              {{- end }}
            {{- end }}
            {{- with .Values.env.extra }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
