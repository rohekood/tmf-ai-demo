apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "qualification.fullname" . }}
  labels:
    {{- include "qualification.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "qualification.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "qualification.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: POSTGRES_URL
              valueFrom:
                secretKeyRef:
                  name: {{ default .Values.env.secretName .Values.env.postgres.secretName }}
                  key: {{ .Values.env.postgres.secretKey }}
            - name: RABBITMQ_URL
              valueFrom:
                secretKeyRef:
                  name: {{ default .Values.env.secretName .Values.env.rabbitmq.secretName }}
                  key: {{ .Values.env.rabbitmq.secretKey }}
            - name: REDIS_ADDR
              {{- if .Values.env.redis.addrSecretKey }}
              valueFrom:
                secretKeyRef:
                  name: {{ default .Values.env.secretName .Values.env.redis.secretName }}
                  key: {{ .Values.env.redis.addrSecretKey }}
              {{- else }}
              value: {{ default .Values.env.redisAddr .Values.env.redis.addr | quote }}
              {{- end }}
            {{- if .Values.env.redis.passwordSecretKey }}
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default .Values.env.secretName .Values.env.redis.secretName }}
                  key: {{ .Values.env.redis.passwordSecretKey }}
            {{- end }}
            {{- with .Values.env.extra }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
