apiVersion: v1
kind: Service
metadata:
  name: {{ include "party-management.fullname" . }}
  labels:
    {{- include "party-management.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "party-management.selectorLabels" . | nindent 4 }}
